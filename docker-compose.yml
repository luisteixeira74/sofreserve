version: "3.9"

services:
  db:
    image: postgres:15
    container_name: sof-reserve-db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: sofreserve
    ports:
      - "5432:5432"